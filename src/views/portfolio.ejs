<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
  <div class="min-h-screen bg-gradient-to-br from-blue-500 to-blue-900 p-4">
    <div class="max-w-2xl mx-auto pt-16 pb-8">
      <div class="text-center mb-8">
        <img
          src="<%= avatar %>"
          alt="Profile"
          class="w-32 h-32 rounded-full mx-auto mb-4 border-4 border-white shadow-lg"
        />
        <h1 class="text-4xl font-extrabold text-white mb-2"><%= title %></h1>
        <p class="text-white/90 text-lg"><%= description %></p>
      </div>

      <div class="space-y-4">
        <% links.forEach((link, index) => { %>
          <a
            href="<%='https://linklytics-backend.onrender.com/' + link.url %>"
            target="_blank"
            rel="noopener noreferrer"
            class="block bg-white/10 backdrop-blur-sm border border-white/30 text-gray-100 p-4 rounded-lg transition-all duration-200 transform hover:bg-white/20 hover:scale-[1.02] hover:shadow-lg"
          >
            <div class="flex items-center justify-center w-full">
              <span class="font-medium text-3xl"><%= link.title %></span>
            </div>
          </a>
        <% }); %>
      </div>

      <footer class="mt-12 text-center text-white/70 text-sm">

      </footer>
    </div>
  </div>
</body>
</html>
