<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="min-h-screen bg-gradient-to-br from-blue-600 via-blue-400 to-cyan-300 p-4">
        <div class="max-w-2xl mx-auto pt-16 pb-8">
            <div class="text-center mb-8">
                <img
                    src="<%= avatar %>"
                    alt="Profile"
                    class="w-32 h-32 rounded-full mx-auto mb-4 border-4 border-white shadow-lg"
                />
                <h1 class="text-3xl font-bold text-white mb-2"><%= title %></h1>
                <p class="text-white/90">Digital Creator & Developer</p>
            </div>

            <div class="space-y-4">
                <% links.forEach((link, index) => { %>
                    <a
                        href="<%= link.url %>"
                        target="_blank"
                        rel="noopener noreferrer"
                        class="block bg-white/10 backdrop-blur-sm hover:bg-white/20 text-white p-4 rounded-lg transition-all duration-200 transform hover:scale-[1.02] hover:shadow-lg"
                    >
                        <div class="flex items-center justify-between">
                            <span class="font-medium"><%= link.title %></span>
                        </div>
                    </a>
                <% }); %>
            </div>

            <footer class="mt-12 text-center text-white/70 text-sm">
                © <%= new Date().getFullYear() %> Jane Doe. All rights reserved.
            </footer>
        </div>
    </div>
</body>
</html